- Загрузить файл с заданием в новый датафрейм Pandas

- Векторизовать тексты (столбец body, из загруженного датафрейма):

  -векторизовать с использованием библиотеки re

  -векторизовать с использованием библиотеки collections и объекта Counter

  -векторизовать с использованием библиотеки sklearn и метода CountVectorizer

  -векторизовать с использованием библиотеки sklearn и метода TfIdfVectorizer

- Сравнить полученные матрицы на предмет пересечения названий столбцов (токенов) и полученных частот 

- Полученную   с использованием библиотеки collectionsматрицу документ-терм (токен) свернуть в матрицу из двух столбцов, с помощью метода главных компонент (PCA), реализуемого с помощью библиотеки sklearn

- Визуализировать вектора отзывов в новом пространстве главных компонент и диаграммы рассеяния

- Используя матрицу нагрузок (т.е. корреляций между исходными векторами, и полученными главными компонентами) интерпретировать полученные две компоненты

- Используя шаблоны регулярных выражений и морфологический анализатор из библиотеки pymorphy2, оставить только прилагательные при векторизации отзывов

-*Оставить только прилагательные, характеризующие позитивную и негативную тональность, например, с помощью открытых датасетов русских негативных и позитивных слов.

- Перестроить главные компоненты и визуализацию на основе новых векторов.

- По полученной визуализации сравнить кластеры плохих и хороших отзывов с оценками из поля stars исходного датафрейма

- Результирующий датафрейм (документ-терм) в виде xls файла,  текстовый файл с ссылкой на колаб с результатами добавить в архив со своими фио и загрузить в результаты ПР2. Результаты
